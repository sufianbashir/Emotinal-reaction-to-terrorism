Google python for windows
download and install python 2.7 , 3.6.7
also install python 3 
Downlaod and install anaconda that we will need for data analysis later.

sudo pip install python3
sudo pip install tornado 
sudo pip install nose
sudo pip install pandas
if you see error related to six and dateutil run following commands 
sudo pip install tld --ignore-installed
sudo pip install pandas --ignore-installed python-dateutil



pip install tweet-preprocessor
pip install fuzzywuzzy
pip install pyspellchecker

#TensorFlow needs python 3.6.7 because it does not install on 3.7 or 3.8 version